[pytest]
testpaths = tests
markers =
    integration: marks tests that require actual model weights (deselect with `-m "not integration"`)

# Default: run unit tests only. To run all including integration:
#   .venv312/bin/python -m pytest -m integration -v -s
addopts = -m "not integration"
