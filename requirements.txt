# VerifyLens — Python dependencies
# Python >= 3.12 (Apple Silicon / MLX) or 3.10+ (CPU-only)
#
# Install in a venv:
#   python3.12 -m venv .venv312
#   source .venv312/bin/activate
#   pip install -r requirements.txt

# ── Apple Silicon inference (MLX) ─────────────────────────────
# Both approaches use MLX for on-device inference.
mlx>=0.31.2
mlx-lm>=0.31.3          # Approach 1: text LLM (Qwen2.5-1.5B) via LoRA
mlx-vlm>=0.6.15         # Approach 2: multimodal VLM (Qwen2.5-VL-3B)

# ── PyTorch (for face verification + CLIP classifier) ─────────
torch>=2.1.0
torchvision>=0.16.0

# ── HuggingFace ecosystem (model hub access, tokenizers) ──────
transformers>=5.14.0
huggingface-hub>=1.24.0
tokenizers>=0.22.0
safetensors>=0.4.0

# ── Document classification ────────────────────────────────────
open-clip-torch>=2.24.0

# ── Face verification ──────────────────────────────────────────
facenet-pytorch>=2.6.0   # MTCNN + InceptionResnetV1

# ── OCR (Approach 1 only) ─────────────────────────────────────
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# ── API & serving ──────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.8.0

# ── Image processing ───────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0

# ── Utilities ──────────────────────────────────────────────────
pyyaml>=6.0
tqdm>=4.66.0

# ── Testing ────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
